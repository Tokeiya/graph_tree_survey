[package]
name = "driver"
version = "0.1.0"
edition = "2024"

[dev-dependencies]

[dependencies]
graph = { path = "../graph" }
tree = { path = "../tree" }
shared = { path = "../shared" }
rand = "*"
rand_core = "*"
mockall = "*"
